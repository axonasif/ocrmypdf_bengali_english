# ocrmypdf bengali+english tessdata_best

Just open this repo in Gitpod: https://gitpod.io/from-referrer/

Then you can run `docker run --rm -i ocrmypdf -l ben+eng - - < input.pdf > out.pdf` on the terminal.